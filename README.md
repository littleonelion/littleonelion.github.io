
```
apt update && apt install -y git wget vim
git config --global user.email s@n && git config --global user.name "Sl"
git clone --branch main https://github.com/sleepycangaroo/sleepycangaroo.github.io.git

git add . && git commit -m "updates" && git push origin main

